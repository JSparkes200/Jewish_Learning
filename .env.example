# Copy to `.env` in this folder (repo root). Do not commit `.env`.
# Used by: `py -3.12 scripts/ingest.py` (python-dotenv loads from repo root).

OPENAI_API_KEY=

# Next.js /api/rabbi also reads `web/.env.local` — same key is fine locally.
